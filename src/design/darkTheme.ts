import { theme } from 'ant-design-vue';

const dark = {
  algorithm: theme.darkAlgorithm,
  token: {
    // Seed Token
    colorPrimary: '#1890ff',
    // Map Token
    colorPrimaryBg: '#e6f7ff',
    // Alias Token
    colorLink: '#1890ff',
  }
};

export default dark;
